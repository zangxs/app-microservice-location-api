package com.brayanpv.app.model.request;

public record LandscapeRequest(String title,
                               String description,
                               Double latitude,
                               Double longitude) {
}
